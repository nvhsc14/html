<!DOCTYPE html>
<html>
<head>
    <titile>simple To-Do List</titile>

    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f2f2f2;
            text-align: center;

        }

        .container {
            background: white;
            width: 300px;
            margin: 50px auto;
            padding: 20px;
            border-radius: 5px;
        }

        input {
            width: 705;
            padding: 5px;
        }

        button {
            padding: 5px 10px;
            cursor: pointer;
        }

        v1 {
            list-style: none;
            padding: 0;
        }

        li {
            background: #eee;
            margin: 5px 0;
            padding: 5px;
        }

        .delete {
            float: right;
            color: red;
            cursor: pointer;
        }
    </style>
</head>

<body>

    <div class="container">
        <h2>To-Do List</h2>

        <input type="text" id="textInput" placeholder="Enter task">
        <button onclick="addText()">Add</button>

        <v1 id="tasklist"></v1>
    </div>

    <script>
        function addText() {
            var task = document.getElementById("taskInput").value;
            
            if (task === "") {
                alert("Please enter a text");
                return;
            }

            var li = document.createElement("li");
            li.innerHTML = task + "<open class= 'delete' onclick= 'removeTask(this)'>Xc</open";

            document.getElementById("tasklist").appendChild(li);
            document.getElementById("taskInput").value = "";
        }

        function removeTask(element) {
            element.parentElement.remove();
        }
    </script>

</body>
</html>